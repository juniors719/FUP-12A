#include <stdio.h>
#include <string.h>
int main(){
    // programa que lê uma frase, uma palavra e uma substring e substitui a palavra pela substring na frase
    char frase[100];
    scanf("%[^\n]s", frase);
    char palavra[20];
    scanf(" %[^\n]s", palavra);
    char sub[20];
    scanf(" %[^\n]s", sub);
    char novaFrase[100];
    int i = 0, j = 0, k = 0, l = 0, m = 0;
    
    printf("%s\n", novaFrase);

}